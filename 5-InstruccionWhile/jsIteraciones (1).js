function mostrar()
{
	var numero;

	numero = 0;

	while (numero < 10)
	{
		numero = numero + 1;
		console.log(numero);
	}
	


}//FIN DE LA FUNCIÓN

//while. para que vuelva a evaluar.
//cuando tengo que evaluar infinitas veces.
//cuando termina el renglon o la llave, repite.
//para que una vez que entro salga, adentro de la llave tengo que cambiar el parametro.

/*while(edad < 17)
{
	//preguntar edad. mientras sea menor a 17 va a volver a preguntar infinitaente.
	//Tiene que modificar su propia condicion para no quedar en bucle infinito.
	//siempre evalua, y despues hace.
}*/
/*primera instruccion que, a diferencia del if y el switch, una vez que termina repite
-tres puntos que tenemos que tener en todo while:
	- tengo que asegurarme de que cuando evalue la info las variables tengan valor correcto.(tengo que cargarle un valor a la variable(inicializarla) antes de empezar)
	- comparacion
	- casi siempre modifica la primera variable.

*/
